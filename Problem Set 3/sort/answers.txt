sort1 uses: Bubble Sort.

How do you know?: Sort1 performs slowly on random lists. But performs swiftly on sorted lists.

sort2 uses: Merge Sort.

How do you know?: Sort2 performs the fastest on random lists.

sort3 uses: Selection Sort.

How do you know?: Sort3 runs faster on random lists than sort1 (bubble sort) but it is slower than sort2 (merge sort).
